import 'package:flutter/material.dart';

class StoreStore extends StatelessWidget {
  const StoreStore({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Coming soon'),
      ),
    );
  }
}
