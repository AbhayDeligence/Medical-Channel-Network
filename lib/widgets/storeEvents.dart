import 'package:flutter/material.dart';

class StoreEvents extends StatelessWidget {
  const StoreEvents({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Coming soon'),
      ),
    );
  }
}
